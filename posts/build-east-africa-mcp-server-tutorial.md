---
title: "Build Your First East Africa MCP Server in 30 Minutes"
tags: africa, ai, mcp
---

The scaffold every tool in this portfolio started from:

**pyproject.toml** (most important):
```toml
[build-system]
build-backend = "setuptools.build_meta"  # EXACT string
```

**Release**:
```bash
git tag v0.1.0 && git push origin v0.1.0
```

GitHub Actions → pip install your-mcp-server → live in 2-3 minutes.

[gabrielmahia.github.io](https://gabrielmahia.github.io) · 12 packages live
